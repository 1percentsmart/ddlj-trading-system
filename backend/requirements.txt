# ============================================================
# DDLJ Trading System — Python Dependencies
# ============================================================
# Install with: pip install -r requirements.txt
# ============================================================

# ── Web Framework ──
fastapi>=0.110.0              # Modern async Python web framework
uvicorn[standard]>=0.29.0     # ASGI server (runs FastAPI)
python-multipart>=0.0.9       # File upload support for FastAPI
websockets>=12.0              # WebSocket support

# ── Environment & Config ──
python-dotenv>=1.0.0          # Load .env files

# ── Pydantic (Data Validation) ──
pydantic>=2.0.0               # Data validation (bundled with FastAPI)

# ── Kite API ──
kiteconnect>=4.1.0            # Zerodha Kite Connect Python SDK

# ── Data & Time ──
pytz>=2024.1                  # Timezone handling

# ── Database ──
sqlalchemy[asyncio]>=2.0.0    # ORM with async support
aiosqlite>=0.20.0             # Async SQLite driver (local dev fallback)
# asyncpg>=0.29.0             # Async PostgreSQL driver (Supabase production)
# supabase>=2.0.0             # Supabase Python client (future)

# ── HTTP Client (Telegram notifications) ──
httpx>=0.27.0                 # Async HTTP client for Telegram API

# ── Error Tracking (Future — Sentry) ──
# sentry-sdk[fastapi]>=2.0.0  # Error tracking

# ── Testing ──
pytest>=8.0.0                 # Testing framework
pytest-asyncio>=0.23.0        # Async test support
httpx>=0.27.0                 # Also used for TestClient
